import { Button, Container } from 'react-bootstrap';

function App() {
  return (
    <Container className="text-center mt-5">
      <h1 className="mb-4">💻 IdealPC</h1>
      <p>Proyecto inicial con Vite + React + Bootstrap</p>
      <Button variant="primary">Probar Bootstrap</Button>
    </Container>
  );
}

export default App;
